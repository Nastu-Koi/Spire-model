# STS2 全流程动作核对表与引擎接口映射

状态：**供逐项审阅的动作规格；已做源码／本机游戏 DLL 静态核对，尚未实现或通过运行验收。** 核对日期：2026-09-20。

本文承接 [ARCHITECTURE.md](ARCHITECTURE.md) 第 3、7、12 节。重点事件清单见 [EVENT_ACTIONS.md](EVENT_ACTIONS.md)，开局、远古之民与结局见 [ANCIENT_ACTIONS.md](ANCIENT_ACTIONS.md)。方框留给人工核对，不表示已经测试通过。

本表列的是引擎决策边界与执行入口；模型应学习的动作原语、数值、目标和组合后果见 [ACTION_ENCODING.md](ACTION_ENCODING.md)。`choose_option` 或按钮文字本身不构成完整的动作语义编码。

## 1. 范围、版本与读法

第一版范围为单人、五角色、固定 A10、正常对局到第三幕 Boss。角色选择、seed、难度与存档管理是环境配置；联机投票、其他玩家操作、日常挑战／自定义修改器另列，不混入首版候选。原版允许但收益差、会受伤甚至会死亡的动作仍属于合法动作。

“所有动作”按 **语义动作类型＋完整参数绑定＋当前子阶段** 穷尽，不为每张卡牌重新发明一个 verb。手牌实例、敌人、药水、奖励、图块与选择前缀随对局变化，无法用一张固定整数动作表列完；运行时必须由引擎完整枚举。

| 证据 | 本次固定值／用途 |
| --- | --- |
| 架构 | 本仓库 `ARCHITECTURE.md`，待实现设计 |
| sts2-cli | `084d1aa3d8e118ca7ce8d8774ad16d6be9c92367`，与架构指定基线一致 |
| 本机游戏 | `release_info.json`：`v0.111.0`、commit `41cef1ea` |
| 本机 `sts2.dll` SHA-256 | `0861bfa1df347538d932f22d580e75420f08082792eb914e53b4882764acdbe9` |
| 中文事件表 SHA-256 | `b3d53173c4a12631b8b0f107de8dc1897d6b22f3b3c2085699eb78b4f0f04885` |
| 验证方式 | 读取 CLI 源码、本地中文表；只读反编译本机 DLL，检查事件、选牌、奖励、营火及特殊 UI 的规则边界；没有修改游戏安装 |

出处：[固定版本 CLI 源码](https://github.com/wuhao21/sts2-cli/blob/084d1aa3d8e118ca7ce8d8774ad16d6be9c92367/src/Sts2Headless/RunSimulator.cs)、[命令入口](https://github.com/wuhao21/sts2-cli/blob/084d1aa3d8e118ca7ce8d8774ad16d6be9c92367/src/Sts2Headless/Program.cs)、[事件文本](https://github.com/wuhao21/sts2-cli/blob/084d1aa3d8e118ca7ce8d8774ad16d6be9c92367/localization_zhs/events.json)。游戏内部符号的证据为上述本机 DLL，不把临时反编译文件作为项目依赖或提交完整反编译代码。

标记：**已有入口**只表示 CLI 能接收相应命令；**需改造**表示现有行为不足以满足架构；**新增桥接**表示必须补出决策／执行入口。下文大写动作名均为拟议协议语义，**不是已实现 CLI 命令**。

## 2. 全局候选规则

- [ ] A-01：公开状态与完整合法候选来自同一稳定快照；生成候选不消耗 RNG、不重新抽事件选项。
- [ ] A-02：0 候选必须区分等待、终局、明确无决策自动流程和协议错误；不能自动离开未知事件。
- [ ] A-03：只有 1 个合法语义候选时自动执行；计数包含结束、跳过、替代奖励与当前可合法使用／丢弃的药水。
- [ ] A-04：至少 2 个候选时交模型选择；不能按策略偏好删掉危险动作或自动领取奖励。
- [ ] A-05：事件 `IsLocked` 是锁定；`WillKillPlayer` 是危险提示。二者不等价。
- [ ] A-06：命令绑定真实实例；相同名称、`TextKey` 或内容 ID 的不同实例不合并。药水的列表索引与物理槽位不能混淆。
- [ ] A-07：只能去重已证明语义严格等价的 UI 别名，不能按相同收益或隐藏随机结果去重。
- [ ] A-08：执行检查 `decision_id/state_version/selection_revision`，旧帧、重复命令、非法参数不改动状态、不支付部分成本。
- [ ] A-09：任意动作都可能挂起到子选择；保持来源、已支付成本及 continuation，子流程后准确恢复。
- [ ] A-10：`IsAllowed`／事件池决定“能否生成该事件”；页面 `CurrentOptions` 与交互规则决定“当前能做什么”。不能在事件中重新套入场条件删除已经存在的按钮。

## 3. 地图、战斗与药水

| 核对号 | 动作与完整绑定 | 当前合法条件／后续 | CLI 兼容点 |
| --- | --- | --- | --- |
| [ ] M-01 | `SELECT_MAP_NODE(node_ref)` | 每个当前可到达节点一个候选；包括特殊移动规则允许的节点；未揭示问号结果不得作为候选属性 | `select_map_node(col,row)`；需增加真实可达性与版本校验 |
| [ ] M-02 | 跨幕／前往已确定下一房间 | 存在路线分支仍为 M-01；仅无选择的进入、治疗、地图生成自动推进 | `proceed`／内部转场，需去除猜测性强制转场 |
| [ ] C-01 | `PLAY_CARD(card_ref,target_ref?)` | 手中每个可打实例 × 每个合法目标；费用、星星、目标限制、角色机制与效果钩子以引擎为准 | `play_card(card_index,target_index?)`；需枚举完整绑定 |
| [ ] C-02 | `END_TURN` | 正常可结束玩家回合且没有阻塞子选择；有可打牌时也保留 | `end_turn`；不能把结算等待当可结束回合 |
| [ ] C-03 | 出牌触发选牌、生成卡三选一、弃牌、消耗、回收、排序 | 转第 6 节；父卡费用与前序效果只执行一次 | `select_cards`／奖励入口仅部分覆盖，需统一会话 |
| [ ] P-01 | `USE_POTION(potion_ref,target_ref?)` | 按 Usage、目标、当前房间、`CanUseOrRemovePotions` 和 custom usability 判断；不限于战斗阶段 | `use_potion(potion_index,target_index?)`；需改造 |
| [ ] P-02 | `DISCARD_POTION(potion_ref)` | 可移除且当前交互允许；逐个实例枚举，保留腾槽与使用的差别 | `discard_potion(potion_index)`；需补权限检查 |
| [ ] P-03 | 污浊药水投向真／假商人 | `TargetedNoCreature`；关闭库存后向商人投掷是组合执行；真商人与假商人有不同后果 | 当前常规目标接口不足，需商人语义目标桥接 |
| [ ] P-04 | 战斗外药水、死亡触发药水 | 只有主动可用药水产生 P-01；自动触发由引擎结算，不产生“点击复活”动作 | 不按所有库存药水一律生成“使用” |

`TargetType` 已核对：`None`、`Self`、`AnyEnemy`、`AllEnemies`、`RandomEnemy`、`AnyPlayer`、`AnyAlly`、`AllAllies`、`TargetedNoCreature`、`Osty`。随机敌人与全体敌人不由模型选一个具体敌人；单人 `AnyPlayer` 绑定自己；联机限定的 `AnyAlly` 不能凭空生成目标。药水 `Self` 的引擎参数与卡牌不同，应由各自 binder 正确绑定，不能所有非敌人目标都传 null。

五角色共享这些入口。星星、球、锻造、奥斯提等属于公开资源／效果；只有规则真正要求选择对象、位置或模式时，才新增对应参数或子选择，不能伪造“任意操纵召唤物／球”的独立动作。

## 4. 商店、宝箱与奖励

| 核对号 | 语义动作 | 合法性与需要保留的选择 | CLI 兼容点 |
| --- | --- | --- | --- |
| [ ] S-01 | `BUY_CARD(item_ref)` | 当前有库存、能支付且规则允许购买；同名不同商品分别列出 | `buy_card(card_index)` |
| [ ] S-02 | `BUY_RELIC(item_ref)` | 同上；获得遗物可能触发附魔、选牌、选卡包 | `buy_relic(relic_index)`，已有部分挂起桥接 |
| [ ] S-03 | `BUY_POTION(item_ref)` | 同上＋容量／可接收条件；槽满时先保留合法的丢弃／使用途径 | `buy_potion(potion_index)`；不能吞异常后报告成功 |
| [ ] S-04 | `REMOVE_CARD(service_ref,card_ref)` | 服务可用、可支付、牌可移除；若先付款后选牌则使用父子会话；若仅无代价打开预览，可直接绑定最终牌 | `remove_card`→`select_cards(indices)`；需补提交／取消时机 |
| [ ] S-05 | `LEAVE_SHOP` | 放弃剩余购买机会；不能当纯 UI 自动执行 | `leave_room` 须限制到真实退出权限 |
| [ ] S-06 | 商店中 P-01／P-02／P-03 | 与购买、移除、离开同帧联合枚举；每次购买／药水效果后刷新库存及价格 | 新候选提供器 |
| [ ] T-01 | `TAKE_RELIC(relic_ref)` | 宝箱／遗物选择页面的每个真实可拿实例；有多个不得固定拿第 0 件 | 基线 `TreasureState` 自动第 0 件，需改造 |
| [ ] T-02 | `SKIP_REWARD(group_ref)`／`LEAVE_REWARDS` | 只在原规则允许时；放弃有副作用的遗物／其余奖励是决策 | 新增统一奖励会话 |
| [ ] R-01 | `CLAIM_REWARD(reward_ref)` | 金币、药水、遗物等各自的领取；领取顺序可能触发钩子或腾槽，不默认排序 | 基线自动领取非卡奖励，需改造 |
| [ ] R-02 | `TAKE_CARD_REWARD(group_ref,card_ref)` | 选当前已展示的牌；不提前读取未开奖励组的牌面 | `select_card_reward(card_index)` |
| [ ] R-03 | 卡牌奖励“跳过／暂时返回” | 区分结束当前选择但奖励未完成，以及最终放弃该组／离开奖励页；依 `AfterSelected` 原语义 | `skip_card_reward` 信息不足，需保留生命周期 |
| [ ] R-04 | `CHOOSE_REWARD_ALTERNATIVE(group_ref,option_ref)` | 每个当前 alternative 独立候选，例如重掷、献祭、受遗物修改的跳过收益；按完整对象处理 | 基线接收 `alternatives` 但未完整保留，新增映射 |
| [ ] R-05 | 重掷后的再选 | 真实执行重掷后形成新帧；只能看到新公开牌；不能预知、重复使用已耗次数 | 与 R-04 共用 verb |
| [ ] R-06 | `SELECT_BUNDLE(bundle_ref)` | 卡包整体互斥选择；逐包展示已公开组成；不是挑包里的任意单牌 | `select_bundle(bundle_index)`；需拒绝越界，不能回退包 0 |
| [ ] R-07 | 同房间多组奖励 | 逐个维护 pending/completed/skipped；组间次序可选时需枚举；组内选牌不自动完成其他组 | 基线只挑第一卡牌奖励的逻辑需审计重写 |
| [ ] R-08 | 药水奖励槽满 | 有位置才领取，或先执行当前合法的 P-01/P-02；只有原界面有替换操作时才绑定替换 | 禁止自动丢弃现有药水或默默丢掉新药水 |

源码的 `CardRewardAlternative.Generate` 会先按 `CanSkip/CanReroll` 建列表，再运行效果钩子。其 `AfterSelected` 可以是继续本页面、完成奖励、仅离开选择但不完成奖励，不能统称“跳过”。无后果的反复打开／关闭奖励 UI 不进入策略；仍可领取的奖励组保留在统一奖励边界中。

## 5. 营火动作逐项

这些动作都统一表示 `CHOOSE_REST_OPTION(option_ref)`，当前实际 `RestSiteOption.Generate` 和 `IsEnabled` 决定出现哪些。不是所有角色每次都拥有整张表。

| 核对号 | OptionId | 动作／子选择 | 合法条件要点 |
| --- | --- | --- | --- |
| [ ] F-01 | `HEAL` | 休息，按实际值治疗及触发效果 | 选项存在且启用；不能因满血自行剔除 |
| [ ] F-02 | `SMITH` | 选择可升级牌，数量 `SmithCount`，再升级 | 可升级牌非空；保留原规则的可取消选择 |
| [ ] F-03 | `DIG` | 挖掘遗物 | 来源遗物／规则提供；获得遗物可有子交互 |
| [ ] F-04 | `LIFT` | 增加后续战斗的力量收益 | 来源提供且仍有使用次数 |
| [ ] F-05 | `COOK` | 选 2 张可移除牌，移除并增加最大生命 | 至少 2 张可移除牌；取消不等于完成烹饪 |
| [ ] F-06 | `HATCH` | 孵化蛋 | 由持有蛋及相关效果提供 |
| [ ] F-07 | `KINDLE` | 给对应遗物补充充能 | 由相应遗物和当前规则提供 |
| [ ] F-08 | `CLONE` | 复制所有指定附魔的牌 | 由相应规则提供；不是逐牌任选复制 |
| [ ] F-09 | `MEND` | 为其他玩家治疗并绑定目标 | 联机动作；单人首版不生成 |
| [ ] F-10 | 结束／离开营火 | 仅原规则允许结束且会放弃剩余机会时为候选 | 已完成且无剩余机会时自动转场 |

基线统一通过 `choose_option(option_index)` 调用，但非锻造和选牌完成后有强制返回地图逻辑。须按实际剩余营火机会推进，不能假定一次操作永远结束营火。

## 6. 公共子选择：每一种操作都保留来源和去向

| 核对号 | `operation` | 要逐项枚举的对象与含义 |
| --- | --- | --- |
| [ ] Q-01 | `discard` | 从原效果指定区域选牌弃置；手牌触发按实际顺序结算 |
| [ ] Q-02 | `exhaust` | 临时消耗指定战斗牌；与永久移除区别 |
| [ ] Q-03 | `remove` | 从永久牌组移除；使用 `IsRemovable` 与来源过滤 |
| [ ] Q-04 | `upgrade` | 升级指定牌；保留升级预览及 `IsUpgradable` |
| [ ] Q-05 | `transform` | 选将被变化的原牌；目标池由引擎决定，随机结果不是动作参数 |
| [ ] Q-06 | `transform_to` | 指定目标牌的变化，绑定来源与已公开目标，如木雕 |
| [ ] Q-07 | `enchant` | 选符合具体附魔 `CanEnchant` 的牌；附魔类型、层数和附加过滤必须保留 |
| [ ] Q-08 | `copy` | 选复制对象；不得把原规则“复制全部”变成任选子集 |
| [ ] Q-09 | `obtain` | 从已生成公开卡牌网格选加入牌组的牌，例如脑蛭、满屋芝士 |
| [ ] Q-10 | `choose_generated_card` | 战斗／药水生成的候选牌选一；去向可能是手牌，不能误用永久牌组奖励 |
| [ ] Q-11 | `retrieve`／`move` | 从弃牌、消耗、抽牌堆等选牌移到指定区域；公开范围由原效果决定 |
| [ ] Q-12 | `put_on_top`／`put_on_bottom`／`reorder` | 若原效果要求顺序，前缀有序；记录顶部／底部的方向与每次结算时机 |
| [ ] Q-13 | `choose_mode`／`choose_option` | 来自牌、遗物、事件的离散模式；带具体效果与来源；只生成真实存在的模式 |
| [ ] Q-14 | `select_target` | 效果中途才要求的对象选择，使用当前目标集，不能拿父动作时的目标集复用 |

Q-08、Q-11～Q-14 是按实际来源调用实例化的通用操作，不表示每个内容对象都有这些按钮。固定版本的实际调用族包括 `CardSelectCmd.FromChooseACardScreen`、`FromSimpleGridForRewards`、`FromSimpleGrid`、`FromCombatPile`、`FromDeckForUpgrade/Transformation/Enchantment/Removal/Generic`、`FromHand/FromHandForDiscard/FromHandForUpgrade`、`FromChooseABundleScreen`；注册表新增一种交互调用时必须纳入同一穷尽审计。

另有 `RelicSelectCmd.FromChooseARelicScreen`，映射 T-01 的逐遗物选择；本次没有在实际内容模型中找到直接调用点，不将 API 存在误报为某个正常事件必定出现该页面。原效果主动安装的自动选择器（如 WhisperingEarring 的 `VakuuCardSelector`）仍由原规则执行，不能被统一 headless 选牌器抢走，改成玩家决策。

| 核对号 | 会话候选 | 精确定义 |
| --- | --- | --- |
| [ ] Q-15 | `SELECT_ONE(ref)` | 加入一个当前可选元素；每个前缀重新枚举且保证可完成 |
| [ ] Q-16 | `FINISH_SELECTION` | 提交当前已选前缀，不清空；达到最少数量且满足所有约束才出现 |
| [ ] Q-17 | `SKIP` | 明确放弃当前机会，仅在来源允许时出现 |
| [ ] Q-18 | `CANCEL` | 原规则可取消时回父阶段；仅 UI 撤选不作为模型动作；不可擅自退款 |
| [ ] Q-19 | 强制完成 | 原规则不足全选／仅有一个最终结果时，直接完成并记录，不制造无意义排列 |

`CardSelectorPrefs(prompt,N)` 是恰选 N 的请求，默认 `Cancelable=false`。固定版本若候选不超过要求且无需手动确认，部分 `CardSelectCmd` 路径会直接返回全部实际候选；这属于原实现的不足处理，不能由适配器任意降低要求。特别是附魔路径，还要核对进入函数前后的过滤集合，不能只读一个 min/max 推导可选牌。

恰选 N、至少 1 至多 N、0 至 N、有序选择、允许重复、互斥／配额分别验收。`buffered` 提交前只改前缀，`incremental` 每次真实结算形成新帧。原规则随机升级／移除的对象不列成玩家可选牌。

营火锻造、烹饪等可以返回父页面的选择，要保留原取消结果供执行适配器恢复；若打开／关闭选择页完全没有游戏后果，应规范化为父边界的最终选择，不把反复取消做成策略循环。

## 7. 问号事件、远古之民与自动流程

| 核对号 | 动作族 | 具体清单 |
| --- | --- | --- |
| [ ] E-01 | `CHOOSE_EVENT_OPTION(event_ref,stage,option_ref)` | [57 个事件条目](EVENT_ACTIONS.md)，逐页而非一次汇总所有按钮 |
| [ ] E-02 | 动态事件选项 | 玩偶选择、指定药水交换、遗物配对、抽中的卡牌类别等，必须绑定实例而非仅 `TextKey` |
| [ ] E-03 | `DIVINE(mode,cell_ref)` | 水晶球：小幅／大幅 × 当前可点击图块；详见事件表 |
| [ ] E-04 | 自定义商人事件 | 假商人库存购买、污浊药水触发战斗、合法离开 |
| [ ] E-05 | 特殊确认的最终承诺 | 审判坚持到底触发放弃对局确认；文档保留该事件分支，不能悄悄换为正常继续 |
| [ ] E-06 | 事件战斗／奖励／选牌 | 转公共阶段后再按原 continuation 恢复，不重扣父动作成本 |
| [ ] E-07 | 远古之民 | [8 位远古之民的全部静态选项池与子交互](ANCIENT_ACTIONS.md) |
| [ ] E-08 | 建筑师结局 | 单一路径对话和胜利结算；未承诺的终局结果须由引擎确认 |

纯 UI 白名单：打开免费库存、查看已经公开的牌组与提示、无新承诺的确认、动画、敌方回合、已承诺的奖励结算、无分支转场。若关闭库存是向商人投药水的前置 UI，组合进 P-03 的执行绑定；不能因为当前库存开着就永久漏掉 P-03。

不作为首版策略动作：开始／加载／写存档、退出程序、暂停与设置、修改 seed／难度／角色、设置玩家资源、指定抽牌顺序、强制进入房间、调试胜负。事件规则自身要求的致死／放弃选项是 E-05 的特例；不能用通用菜单放弃替代正常合法动作。

## 8. 现有 17 个 action 命令逐一兼容映射

| 现有 action | 参数 | 拟议对应 | 需要改变的地方 |
| --- | --- | --- | --- |
| `select_map_node` | `col,row` | M-01 | 真实合法节点与版本 |
| `play_card` | `card_index,target_index?` | C-01 | 完整目标、无效目标不得回退自动目标、稳定快照 |
| `end_turn` | 无 | C-02 | 子交互／阶段校验 |
| `choose_option` | `option_index` | F／E／远古之民 | room/stage/source 绑定，当前对象权限 |
| `select_card_reward` | `card_index` | R-02 | 奖励组、领取钩子、完成状态 |
| `skip_card_reward` | 无 | R-03 | 当前来源是否可跳过、是否保留奖励 |
| `buy_card` | `card_index` | S-01 | 库存、购买条件、原子性 |
| `buy_relic` | `relic_index` | S-02 | 完整嵌套 continuation |
| `buy_potion` | `potion_index` | S-03 | 槽位与失败语义 |
| `remove_card` | 无 | S-04 父流程 | 选牌／取消／付款时机 |
| `select_bundle` | `bundle_index` | R-06 | 越界报错，不默认包 0 |
| `select_cards` | `indices`（逗号字符串） | Q 会话最终内部提交 | 对外改为逐项；数量、重复、过滤与原子性 |
| `skip_select` | 无 | Q-17/Q-18 的受控子集 | 禁止一律清空，不能用来实现 Q-16 |
| `use_potion` | `potion_index,target_index?` | P-01/P-03 | 使用限制、非生物目标、失败不得强制丢药 |
| `discard_potion` | `potion_index` | P-02 | 移除权限、实例映射 |
| `leave_room` | 无 | 真实允许退出的 S/E/T/R | 删除任意非战斗房间强行回地图的策略入口 |
| `proceed` | 无 | 白名单推进／明确承诺 | 无分支自动推进，不掩盖未知阶段 |

新增 `advance_to_boundary()` 与 `execute_candidate(...)` 包在这些内部操作之外。训练与部署只暴露候选执行，不允许模型自由调用旧 action 或调试命令。

## 9. 已确认的兼容缺口与验收

| 优先项 | 静态发现 | 必须达到的结果 |
| --- | --- | --- |
| [ ] G-01 | `EventChoiceState` 对空选项直接跳过 | 假商人等 custom 页面进入专用 provider；等待与不支持显式区分 |
| [ ] G-02 | `DoLeaveRoom` 强行回地图 | 只执行原规则存在的离开，不能逃过付费／诅咒／战斗 |
| [ ] G-03 | 没有水晶球小游戏候选／执行桥接 | 逐次 mode×cell、公开雾层、完整揭示与最终奖励 |
| [ ] G-04 | 通用选牌过滤越界、允许重复／数量不完整校验 | 拒绝非法输入，不悄悄缩短；STOP 提交已有选择 |
| [ ] G-05 | 奖励 selector 忽略 alternatives 且事件奖励导出固定可跳过 | 使用真实 alternatives／CanSkip；超时不得当跳过 |
| [ ] G-06 | 非卡奖励自动领取、宝箱默认第 0 件 | 完整领取／放弃／顺序选择；唯一候选才自动 |
| [ ] G-07 | 只凭 min/max 无法还原原选择来源与取消规则 | 捕获原 `CardSelectorPrefs`、过滤器、操作、去向、父来源 |
| [ ] G-08 | 事件药水冻结、商人 UI 目标依赖 | 兼容 `CanUseOrRemovePotions` 与非生物目标；别把缺 UI 当规则禁止 |
| [ ] G-09 | 正常营火／战后强制地图转场 | 保留多次机会、事件战斗 Resume、多组奖励与原终局流程 |
| [ ] G-10 | 仅有命令与状态，无统一候选版本协议 | 候选完备性、枚举／执行一致、去重、旧帧拒绝、零 RNG 查询 |

- [ ] 每个动作族至少构造一个真实可达场景，比较 GUI 规则边界与 headless 候选，不用文档推导动作替代引擎枚举。
- [ ] [事件表](EVENT_ACTIONS.md) 每项的父分支、所有子选择、锁定／边界条件均有记录。
- [ ] 付费后死亡、得到诅咒后无可选牌、槽满、同名多实例、同 `TextKey` 多选项、重复页面各有覆盖。
- [ ] 多组奖励、重掷、献祭、暂时退出奖励选择、再领取均保持原规则。
- [ ] 水晶球、假商人、审判确认弹窗不依赖静默跳过或默认选择。
- [ ] A10 五角色整局回归与定向场景分别报告；静态核对、运行通过、人工认可三者分别记账。

本次交付完成动作规格与静态核对，不把上述改造写成已经实现。事件／遗物产生的新交互必须通过统一注册审计才能宣称整个执行引擎兼容。
